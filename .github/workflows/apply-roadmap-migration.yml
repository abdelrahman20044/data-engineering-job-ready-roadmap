name: Apply roadmap migration

on:
  push:
    branches: [main]
    paths:
      - roadmap-migration.tar.gz

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply validated roadmap commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          tar -xzf roadmap-migration.tar.gz
          git am roadmap-migration.patch
          rm roadmap-migration.patch
          git rm roadmap-migration.tar.gz .github/workflows/apply-roadmap-migration.yml
          git commit --amend --no-edit
          git push origin HEAD:main
